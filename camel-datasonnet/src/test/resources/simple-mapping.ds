/** DataSonnet
version=2.5
default=null
output application/json
*/

{
    pid: body.userId,
    name: name,
    age: header.age,
    birthCity: header.birthCity,
    email: email,
    address: {
        street: property.street,
        city: property.city,
        state: property.state,
        zipCode: property.zip
    }
}